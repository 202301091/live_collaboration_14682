events {
    worker_connections 1024;
}

http {
    upstream node_servers {
        server 127.0.0.1:5000;
        server 127.0.0.1:5001;
        server 127.0.0.1:5002;
    }

    server {
        listen 8080;

        location / {
            proxy_pass http://node_servers;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            
            # Preserve origin details
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Prevent proxy buffering issues for long-lived real-time connections
            proxy_buffering off;
            proxy_read_timeout 86400s;
            proxy_send_timeout 86400s;
        }
    }
}
#Check syntax->C:\Users\HP\nginx-1.26.2\nginx.exe -p C:\Users\HP\nginx-1.26.2 -c "$PWD\nginx.conf" -t


# Start->Start-Process -FilePath "C:\Users\HP\nginx-1.26.2\nginx.exe" -ArgumentList "-p C:\Users\HP\nginx-1.26.2 -c `"$PWD\nginx.conf`""

# Stop->taskkill /F /IM nginx.exe